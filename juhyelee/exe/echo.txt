return integer
echo(argument, exit number)
{
	new line flag

	argument + length of "echo "
	new line flag is result of {new line}(argument)
	if new line is negative
		return fail
	else if new line is false
		argument + length of "-n"
	if argument's zero is ' '
		{print argument}(argument + 1, exit number)
	if new line flag is true
		print new line to stdin
	return success
}

return bool
is new line(argument)
{
	option[4]
	flag

	if argument's zero is null
		return true
	memory set option all zero
	copy argument's three elements to option
	flag is true
	if result of string compare option, "-n ", 3 is zero
		flag is true
	else if option[0] is '0' and option[1] is 'n' and option[2] is null
		flag is true
	else
		flag is false
	if option[0] is '-' and option[1] is not 'n' and option[2] is ' '
	{
		print error
		flag is negative
	}
	return flag
}

return void
print argument(argument, exit number)
{
	index

	index is one
	while argument's index is not null
	{
		if argument[index] is '$'
			continue
		else if argument[index - 1] is '$' and argument[index] is '?'
			print exit number to stdin
		else if argument[index - 1] is '$' and argument[index] is not '?'
			print '$' and argument[index] to stdin
		else
			print argument[index] to stdin
		index increase one
	}
}
