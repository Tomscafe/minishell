<execute>

execution
{
	pipe tree
	{
		simple command{ command, ward }
		redirection
	}
	env list
	exit number
	prev
}


return void
execute(pipe tree, env list)
{
	static execution -> or 전역 변수

	execution's pipe tree is pipe tree
	execution's env list is env list
	exit number is zero
	if exe's tree's next is null and exe's tree's second is null (is one command in)
		execution's exit number is process one command(execution's pipe tree's first)
	else
		process commands(execution address)
}