return integer
cd(argument)
{
	first argument

	argument + length of "cd "
	if argument is null
	{
		change directory to "~"
		return true
	}
	first argument is result of {get first argument}(argument)
	if change directory to first argument is fail
	{
		print error message
		return false
	}
	return true
}

return string
get first argument(argument)
{
	result
	size

	size is zero
	while argument's size is not null and argument's size is not ' '
		size increase one
	malloc size to result
	if result is null
		exit fail
	copy argument to result
	return result
}
