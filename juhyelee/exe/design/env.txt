return integer
env(env list)
{
	while env list is not null
	{
		if env list's value is not null
			print env list's variable and '=' and ev list's value and '\n'
		env list is env list's next
	}
	return success
}
